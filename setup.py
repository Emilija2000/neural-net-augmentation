from setuptools import setup

setup(name='neural-net-augmentations',
      version='1.0.0',
      packages=["augmentations"],
      install_requires=[
        "jax",
        "numpy",
        ],
    )
